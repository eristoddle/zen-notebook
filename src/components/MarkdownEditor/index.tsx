import React, { FC } from 'react';
import { Text, View } from 'react-native';

const MarkdownEditor: FC = () => {
  return (
    <View>
      <Text>Markdown Editor</Text>
    </View>
  );
};

export default MarkdownEditor;
